// A - O
// P - Z
var city = 'New York'

// strings can be compared using relational operators
if( city < "P" ) {
    console.log( city );
    console.log( "Look in Volume 1")
} else {
    console.log( "Try again" );
}

newArray = []
newArray = array[i]

// s - z ; result will be [ ]